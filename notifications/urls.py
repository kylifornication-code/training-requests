from django.urls import path
from . import views

app_name = 'notifications'

urlpatterns = [
    # Notifications are handled through signals and services
    # This is a placeholder for future notification-specific views
]